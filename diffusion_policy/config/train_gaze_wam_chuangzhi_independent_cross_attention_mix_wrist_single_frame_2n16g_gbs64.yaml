defaults:
  - train_gaze_wam_chuangzhi_mix_wrist_single_frame_all_gaze_heatmap_1n8g_gbs64
  - _self_

# Keep the corrected all-valid gaze supervision and init checkpoint from the
# latest wrist run while scaling the per-process microbatch to 16 GPUs.
name: train_gaze_wam_chuangzhi_independent_cross_attention_mix_wrist_single_frame_2n16g_gbs64
exp_name: "chuangzhi_independent_cross_attention_mix_wrist_single_frame_temporal_mixed_nll_2n16g_gbs64"

data_mixing:
  total_batch_size_per_process: 4

robot_dataloader:
  batch_size: 3

open_dataloader:
  batch_size: 1

val_robot_dataloader:
  batch_size: 3

val_open_dataloader:
  batch_size: 1
